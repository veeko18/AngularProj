import { Component } from '@angular/core';

@Component({
  selector: 'app-root',
  template: `<app-home> Loading... </app-home>`,
  styleUrls: ['./app.component.css']
})
export class AppComponent {
  title = 'study-client';
}
